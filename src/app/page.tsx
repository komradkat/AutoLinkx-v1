/**
 * SCAFFOLD PLACEHOLDER — Developer A, task A-02.
 *
 * Proves the toolchain runs end to end. The real homepage is B-10; replace
 * this file then. No product UI, fixtures, or data access belong here.
 */
export default function ScaffoldPage() {
  return (
    <main>
      <h1>AutoLinkX scaffold</h1>
      <p>
        Toolchain placeholder. Shared contracts live in <code>src/contracts</code>; no
        marketplace feature is implemented yet.
      </p>
    </main>
  );
}
